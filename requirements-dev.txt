# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
ruff>=0.1.0
PyYAML>=6.0

# Codegen tooling (spec/codegen + spec/runner.py).
# Library users do not need these at runtime — generated files are committed.
Jinja2>=3.0
PyYAML>=6.0

